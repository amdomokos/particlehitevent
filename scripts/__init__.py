"""Orchestration entrypoints (Phase 5). Run as modules from the repo root,
e.g. ``python -m scripts.sweep`` — on a pod, cd to the repo (or export
PYTHONPATH=<repo root>) first so the package is importable."""
